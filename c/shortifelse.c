#include <stdio.h>
int main(){

    // variable = (condition) ? expressionTrue : expressionFalse;
float gpa;
printf("Enter your gpa :");
    scanf("%f",&gpa);
    (gpa==5.00)?printf("You are eligible to exam "):printf(" not eligible ");

    



}